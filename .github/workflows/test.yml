name: Test

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: build
        run: yarn && yarn run build

      - name: run
        uses: ./
        if: always()
        with:
          commitMsg: "🎨 Enforce code style [test]"
          gitName: dothq-robot
          gitEmail: dothq-robot@users.noreply.github.com
          prettierArgs: "--loglevel debug --write ."
